import { render } from '@testing-library/react'
import Main from '.'

describe('Main', () => {
  it('should render the heading', () => {
    //renderiza o componente

    //busca o elemento de heading

    render(<Main />)
    //verifica se o elemento de heading está presente
  })
})
